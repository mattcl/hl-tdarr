---
apiVersion: v1
kind: Service
metadata:
  name: svc
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: server-svc
spec:
  ports:
    - port: 80
      targetPort: server
      protocol: TCP
